version: "3.9"
services:
  api:
    build: .
    container_name: multimodal-search-api
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    command: python -m online.api --config config/settings.yaml --host 0.0.0.0 --port 8000
